# Test configuration for Travner API
# spring.data.mongodb.uri is provided by Testcontainers @ServiceConnection
# spring.data.mongodb.database is provided by container

# Logging configuration for tests
logging.level.org.adnan.travner=DEBUG
logging.level.org.springframework.security=DEBUG

# Disable actuator endpoints in tests
management.endpoints.enabled-by-default=false